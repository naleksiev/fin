int Fib(int n) {
    return n < 2 ? n : Fib(n - 1) + Fib(n - 2);
}

void Main() {
    int n = 32;
    float start = time.Clock();
    io.WriteLine("fib({n}) = {Fib(n)}");
    io.WriteLine("time: {time.Clock() - start}ms");
}
